# set up

  - ```npm install``` @ root


 - ```npm install webpack -g```

-  ```webpack``` (to compile dependencies to dist/app.js)

# optional

  - ```npm test```
  - ``` npm run test:watch```
  - ``` npm install live-server -g```
    - ```npm run devpack```
